-- ============================================================
-- V11: 권한 시스템 완전 삭제 (Permissions System Cleanup)
-- ============================================================

-- 외래 키 제약 조건 때문에 자식 테이블(매핑 테이블)부터 순서대로 지워야 합니다.

-- 1) 직무-권한 매핑 테이블 삭제 (V10에서 생성한 것)
DROP TABLE IF EXISTS job_role_permissions;

-- 2) 권한 마스터 테이블 삭제 (최종 부모 테이블)
DROP TABLE IF EXISTS permissions;